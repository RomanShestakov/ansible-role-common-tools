# tasks/main.yml

- name: Install Common useful tools
  yum:
    name: "{{ item }}"
    state: present
  with_items:
  - git
  - make
  - htop
  - strace
  - openssl-devel
  - unzip
  - bzip2
  - tar
  - gzip
  - wget
  - ncurses-devel
  - texinfo
  - svn
  - python-devel
  - lua
  # when: ansible_os_family == "Centos"
